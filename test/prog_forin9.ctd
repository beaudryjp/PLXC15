   $a_length = 3;
# Comprobacion de rango
   if (0 < 0) goto L0;
   if (3 < 0) goto L0;
   if (3 == 0) goto L0;
   goto L1;
L0:
   error;
   halt;
L1:
   a[0] = 1.3;
# Comprobacion de rango
   if (1 < 0) goto L2;
   if (3 < 1) goto L2;
   if (3 == 1) goto L2;
   goto L3;
L2:
   error;
   halt;
L3:
   a[1] = 3.5;
# Comprobacion de rango
   if (2 < 0) goto L4;
   if (3 < 2) goto L4;
   if (3 == 2) goto L4;
   goto L5;
L4:
   error;
   halt;
L5:
   a[2] = 5.8;
# ERROR: El tipo de la variable y la matriz no coinciden en la sentencia for..in
   error;
   halt;
